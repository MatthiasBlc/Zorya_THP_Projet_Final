<div class="navbar navbar-expand pr-0 navbar-dark border-bottom-2"
                     id="default-navbar"
                     data-primary>
  <% if !user_signed_in? %>
      <%= link_to root_path do %>
      <span class=""><%= image_tag('logoZorya.png', class: 'img-fluid', id: 'logo-nav', alt: 'logo') %></span>
      <% end %>
  <% else %>
  <% end %>
  <!-- Navbar Brand -->
  <%= link_to root_path,  class:"navbar-brand mr-16pt d-lg-none"  do %>
    <span class="rounded bg-primary"><%= image_tag('logoZorya.png', class: 'img-fluid', id: 'logo-nav', alt: 'logo') %></span>
    <span class="d-none d-lg-block">Luma</span>
  <% end %>
  <!-- // END Navbar Brand -->
  <!-- Navbar Toggler -->
  <% if user_signed_in? %>
    <%= button_tag( :class => "navbar-toggler w-auto mr-16pt d-block d-lg-none rounded-0", :type => "button", :data_toggle => "sidebar", )  do %>
      <%= link_to "Mes formations", user_assignments_path(current_user.id), class: "sidebar-link" %>
    <% end %>
    <% if current_user.status_customer? %>
      <%= button_tag( :class => "navbar-toggler w-auto mr-16pt d-block d-lg-none rounded-0", :type => "button", :data_toggle => "sidebar") do %>
        <%= link_to "Mon espace entreprise", company_path(current_user.company.id), class: "sidebar-link" %>
      <% end %>
    <% end %>
  <% else %>
  <% end %>
  <!-- // END Navbar Toggler -->
  <div class="flex"></div>
  <!-- Navbar Menu -->
  <div class="nav navbar-nav flex-nowrap d-flex mr-16pt">
    <div class="nav-item dropdown">
      <a href="#"
                               class="nav-link d-flex align-items-center dropdown-toggle"
                               data-toggle="dropdown"
                               data-caret="false">
        <span class="avatar avatar-sm mr-8pt2">
          <span class="avatar-title rounded-circle bg-primary"><i class="bi bi-person-fill font-size-20pt"></i></span>
        </span>
      </a>
      <div class="dropdown-menu dropdown-menu-right">
        <div class="dropdown-header"><strong>Mon compte</strong></div>
        <% if user_signed_in? %>
          <%= link_to "Mon profil", edit_user_registration_path, class: " uppercase dropdown-item" %>
          <%= link_to "Se déconnecter",  destroy_user_session_path, class: " uppercase dropdown-item" %>
        <% else %>
          <%= link_to "S'inscrire", new_user_registration_path, class: "uppercase dropdown-item" %>
          <%= link_to "Se connecter", new_user_session_path, class: " uppercase dropdown-item" %>
        <% end %>
      </div>
    </div>
  </div>
  <!-- // END Navbar Menu -->
</div>
