<% if @list_user_company_of_user_id.include?(@user.id) %>
<% else %>
  <%= id = "user_list" %>
  document.getElementById("<%= id %>").insertAdjacentHTML("beforeend", "<%= j render 'new_user', user: @user %>")
<% end %>
