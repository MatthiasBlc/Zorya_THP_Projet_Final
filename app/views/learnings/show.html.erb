<% if current_user.learnings.include?(@learning) %>
<%= @learning.content.html_safe %>
  <% else %>
    <h3><%= "Vous n'avez pas accès à cette page"%><h3>
  <% end %>
  
  <%puts @assignment.id%>

<%puts current_user.id%>
<%= button_to "Valider la formation",  user_assignment_path(current_user.id, @assignment.id), params:{user_id: current_user.id, learning_id: @learning.id} ,method: "put", class: "" %>
